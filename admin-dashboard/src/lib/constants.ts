export const isEmailAdmin = async (_email: string | null | undefined) => {
    // This is a placeholder since we've moved the logic to AuthContext directly.
    // The underscore prefix tells TypeScript that the variable is intentionally unused.
    return false;
};
